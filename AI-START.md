# AI-START — 이 저장소를 읽는 AI에게

이 파일은 AI용 진입점이다. 사람용 설명은 `README.md`에 있고, 너는 그것을 읽지 않아도 된다.

## 너의 임무

사용자가 이 저장소를 너에게 건넸다면 목표는 하나다 — **사용자의 지침 파일·설정·작업 습관을 진단하고, 사용자가 승인할 수 있는 최소 변경을 제안한다.**

이 저장소는 사용자 환경에 부어 넣을 규칙 묶음이 아니라 **진단에 쓰는 재료**다. 사용자 환경이 먼저고, 이 저장소는 그다음이다.

## 읽는 순서

1. 이 파일 전체.
2. 사용자 환경 (단계 1).
3. `CHANGELOG.md` — 처음 진단이면 맨 위 판만 본다(출력 머리와 단계 5의 표시에 쓴다). 재진단이면 전체를 읽는다(단계 2-1).
4. 단계 3에서 연 트랙의 `gates.md`만.

읽지 않는 것:
- `cases.md` — 사용자가 "왜 이 제안인가"를 물을 때 해당 ID만 연다.
- `kit/` — 제안이 승인된 뒤 해당 파일만 연다. gate의 "넣는 형태"가 kit 파일을 가리키면 제안에는 파일 이름과 gate에 적힌 설명만 적는다.
- 열지 않은 트랙의 파일 전부.

---

## 단계

### 0. 전제 확인

이 저장소는 **사용자의 작업 폴더를 직접 읽을 수 있는 에이전트**를 전제한다. 파일에 접근할 수 없는 환경(채팅 전용)에서는 단계 1이 성립하지 않는다 — 진단을 시작하지 않고, 이 저장소의 대상 환경이 아니라고 보고한 뒤 끝낸다. **읽지 않은 파일을 읽은 것처럼 쓰지 않는다.**

도구별 파일 위치는 사용자 도구에 맞는 어댑터 하나만 연다 — [claude-code](adapters/claude-code.md) · [agents-md](adapters/agents-md.md).

### 1. 사용자 환경 먼저

이 저장소의 트랙을 열기 전에 읽는다.
- 상시 지침 파일 (어댑터의 "상시 지침" 행). 그 행에 없어도 매 실행마다 통째로 AI에게 들어가는 파일(프롬프트 파일 등)은 상시 지침으로 본다
- 권한·설정 파일, 기억 인덱스 (있으면)
- 사용자가 스스로 만든 규칙 체계 — 표시 태그, 파일 조작 규칙, 검사 스크립트, 기록용 인박스 같은 것
- **이 저장소에서 온 표시** — `(naite SPEC-04 · v0.1.0)` 꼴. 표시마다 ID · 판 · 위치(파일·줄)를 목록으로 적는다. 표시를 찾는 검색에서 자격증명·민감 자료(env 파일, 키 파일, 사용자가 민감하다고 정한 폴더)는 명시적으로 뺀다 — 검색도 내용을 읽는 일이다. 도구의 기본 제외 동작에 기대지 않는다

**읽는 범위**는 위 파일들과, 상시 지침이 경로·링크로 **직접 가리키는** 규칙 문서까지다. 가리킨 문서가 다시 가리키는 문서는 열지 않는다. 진단의 **없음**은 이 범위 안에서 없다는 뜻이고, 출력의 "읽은 것"이 그 범위를 보여 준다.

**없는 것도 기록한다.** "지침 파일 없음", "naite 표시 없음"은 진단 결과다.

### 2. 수준·경로 판정

**수준**
- 상시 지침 파일이 없거나, 있어도 규칙이라 할 만한 문장이 거의 없다 → **[starter.md](starter.md) 경로.** 격차 진단을 하지 않는다 — 비교할 체계가 없다. 대신 트랙마다 **근거 슬롯** 하나를 둔다(단계 3-1).
- 지침 파일에 규칙 체계가 있다 → **격차 진단 경로** (단계 3~5).

경계가 애매하면 진단을 시작하지 않는다. 출력 양식의 머리(수준 `확인 필요`)와 "읽은 것"까지만 쓰고, 수준 판정 줄에 물을 질문을 적은 뒤 답을 기다린다.

**경로** — 수준과 따로 정한다.
- naite 표시가 없다 → **처음 진단.**
- naite 표시가 하나라도 있다 → **재진단** (단계 2-1).

### 2-1. 재진단 — 이미 적용한 적이 있을 때

1. `CHANGELOG.md`를 연다. 표시마다, 그 표시의 판 **뒤에 나온 판들**에서 해당 ID가 변경·폐기됐는지 본다.
2. 표시마다 판정한다.

| 대조 결과 | 판정 | 처리 |
|---|---|---|
| 뒤 판에서 변경·폐기 없음 | **적용됨** | 제안하지 않는다. 표시 주변 문장이 사용자 손으로 바뀌었거나 지워졌어도 다시 제안하지 않는다 — 사용자의 선택이다(금지 1). 보고에만 적는다 |
| 뒤 판에서 변경됨 | **갱신 후보** | 바뀐 점만 제안한다. 사용자 문장을 통째로 갈아 끼우지 않고, 차이와 이유를 보여 준다 |
| 뒤 판에서 폐기됨 | **제거 후보** | 폐기 사유와 함께 **묻는다**. 지우지 않는다 — 사용자가 채택한 순간 그것은 사용자의 규칙이다 |
| 판 표시가 없거나, 그 판이 CHANGELOG에 없다 | **판정 보류** | 갱신 여부를 가리지 않는다. 표시에 판을 붙이자고 알린다 |

3. 표시가 없는 gate는 처음 진단과 같게 판정한다(단계 4). 사용자 환경의 표시 중 **가장 최신 판** 뒤에 추가된 ID를 먼저 본다.
4. 트랙 판정(단계 3)도 다시 한다. 사용자가 하는 일이 바뀌었을 수 있고, 새로 생긴 트랙이 있을 수 있다.
5. 제안 상한 5개는 **갱신 후보 → 제거 질문 → 새로 추가된 ID → 나머지** 순으로 채운다.

### 3. 트랙 판정

사용자가 **실제로 하는 일**에 해당하는 트랙만 연다. 연 트랙은 이유를 한 줄로 적고, 열지 않은 트랙도 이유를 적는다.

| 트랙 | 여는 조건 | 파일 |
|---|---|---|
| spec — 문서 | 사용자가 AI와 함께 기획·정책·보고·위키 문서를 쓰거나 유지한다 | [tracks/spec/gates.md](tracks/spec/gates.md) |
| setup — 환경 | 사용자가 AI 에이전트에게 파일 조작·명령 실행·자격증명이 필요한 호출을 맡기거나, 세션을 넘는 기억을 쓰거나, 같은 요청을 반복한다 | [tracks/setup/gates.md](tracks/setup/gates.md) |

이 저장소에 있는 트랙은 위 표가 전부다. 표에 없는 영역(데이터 분석·무인 자동화 등)을 이 저장소의 이름으로 제안하지 않는다. **열 트랙이 없으면 그렇게 보고하고 끝낸다** — 억지로 여는 것은 금지 3 위반이다.

### 3-1. starter 경로의 근거 슬롯

starter 경로에서는 연 트랙을 전체 진단하지 않는다. 대신 이렇게 한다.

1. starter 항목 S1~S4가 제안 상한을 먼저 채운다. S5는 적재 훅 칸에 쓰고 상한에 세지 않는다(단계 7). 지침 파일 신설 제안(`신설`)도 상한에 세지 않는다 — 규칙이 아니라 S1~S4를 담을 자리다.
2. 남는 자리가 **근거 슬롯**이다. 연 트랙마다 최대 1개의 gate를, 그 gate가 겨냥하는 사례를 사용자 파일에서 **파일·줄로 인용할 수 있을 때만** 제안한다. 인용할 수 없으면 제안하지 않는다.
3. 근거는 단계 1에서 이미 연 파일에서만 찾는다. 사례를 찾으려고 사용자 폴더를 더 뒤지지 않는다.
4. 그 gate의 로딩 조건과 "안 통하는 상황"은 그대로 적용한다.
5. 인용할 사례가 없는 트랙은 보류에 "지침 파일이 생긴 뒤 볼 트랙"으로만 적는다.
6. setup 트랙은 starter 경로에서 열지 않는다 — starter가 그 최소판이다(대응은 `starter.md` 맨 위의 대응표).

### 4. 진단

연 트랙의 gate마다 판정한다. 재진단에서 "적용됨"으로 판정한 gate는 다시 판정하지 않는다.

| 판정 | 기준 |
|---|---|
| **있음** | 사용자 환경에 같은 효과의 규칙이 있다. 위치(파일·줄)를 적는다. 표현이 달라도 효과가 같으면 있음, 이름이 비슷해도 효과가 다르면 있음이 아니다 |
| **없음** | 로딩 조건에 해당하는데 규칙이 없다 |
| **충돌** | 사용자 규칙이 gate와 반대 방향이거나, 이 gate가 다루는 일에 대해 사용자 문서끼리 어긋난다. 양쪽 위치를 적는다 |
| **부분** | 같은 효과의 규칙이 있지만 일부 범위(한 트랙·한 파일·기억에만)에 걸려 있다. 있는 위치를 적고, 빠진 범위만 제안한다 |
| **확인 필요** | 로딩 조건에 해당하는지 파일로 판정할 수 없다(예: 같은 요청을 반복하는가). 제안하지 않고, 사용자에게 물을 질문 한 줄을 보류 칸에 적는다 |
| **해당 없음** | 로딩 조건에 해당하지 않거나, "안 통하는 상황"에 해당한다 |

- "안 통하는 상황"이 `없음 — 논리형`인 gate는 반례가 원리적으로 없다는 뜻이다. 해당 없음은 로딩 조건으로만 판정한다.
- 판정 칸에는 위 표의 값 하나만 쓴다. 병기하거나 새 값을 만들지 않는다. 범위가 한정된 규칙은 **부분**으로 적고 한정 조건(한 경로에만 등)을 근거 칸에 적는다. 규칙 문장 없이 사례·예시에만 있으면 **없음**이고, 그 위치를 근거 칸에 적는다.
- 사용자 환경에 starter 표시(`(naite S2 · 판)` 등)가 있으면, 그 starter 항목에 대응하는 gate는 **있음**으로 본다. 대응표는 `starter.md` 맨 위에 있다.

### 5. 제안

- **없음**은 넣을 문장으로, **부분**은 빠진 범위만 넣을 문장으로 제안한다. **충돌**은 바꾸는 문장이 아니라 **양쪽을 병기하고 어느 쪽을 유지할지 묻는 항목**으로 올린다.
- 한 번에 최대 **5개.** 넘치면 사용자가 가장 자주 하는 작업에 가까운 것부터 고르고(재진단이면 단계 2-1의 순서), 나머지는 보류에 남긴다.
- 항목마다 붙인다: gate ID · 넣을 문장 또는 조각 · 넣을 위치 · 검증된 조건 · 충돌 여부.
- 넣을 문장은 gate의 "넣는 형태"를 따르되 사용자 지침 파일의 말투와 형식에 맞춘다.
- 넣을 문장 끝에 출처와 판을 남긴다: `(naite SPEC-04 · v0.1.0)`. 판은 `CHANGELOG.md` 맨 위의 판이다. **이 표시가 다음 재진단의 기준이 된다** — 사용자에게 지우지 말라고 알린다.

### 6. 승인 · 적용 · 검증

- **사용자가 승인한 항목만 적용한다.** 갱신 후보를 적용할 때는 표시의 판도 함께 올린다.
- 적용한 뒤 확인한다: 파일이 여전히 읽히는가(설정 파일이면 형식 오류가 없는가), 새 문장이 기존 규칙과 모순을 만들지 않았는가.
- 설정은 다음 세션부터 반영되는 도구가 있다. 어댑터를 보고 언제 확인할 수 있는지 사용자에게 알린다.
- 스크립트를 넣었다면 일부러 한 번 깨뜨려서 잡히는지 본다.

### 7. 적재 훅

마지막으로 하나를 제안한다 — 사용자 환경에 "기대가 어긋난 순간"을 한 줄씩 적는 자리. 이미 비슷한 자리가 있으면 제안하지 않는다. 제안 상한 5개에 세지 않는다.

```
YYYY-MM-DD | 기대: ___ | 실제: ___ | 변경: ___
```

위치는 사용자 환경에서 한 곳만 고른다. 같은 유형이 두 번 적히면 그때 규칙으로 올리자는 문장을 함께 넣는다. 이것이 한 번의 개선을 이어지는 개선으로 바꾼다.

---

## 금지 — 다섯 가지

1. **사용자의 기존 규칙이 이긴다.** 충돌은 한쪽을 지우지 않고 병기해 묻는다. 한 번 채택한 naite 문장도 사용자의 규칙이다.
2. **이 저장소의 사건을 사용자 시스템의 사실로 적지 않는다.** `cases.md`의 사건은 한 사람의 환경에서 일어난 일이다. 사용자 문서에는 규칙과 조건만 옮기고 출처 표시를 붙인다.
3. **조건 없이 규칙을 옮기지 않는다.** "안 통하는 상황"에 해당하면 제안하지 않는다. 사용자가 하지 않는 일의 트랙을 열지 않는다.
4. **확신도를 보존한다.** gate의 유래가 `판단형`이면 제안에 "판단형 — 겪은 사건 없음"을 적는다. "안 통하는 상황" 칸은 판단에서 왔다 — 사용자 환경에서 반례가 보이면 사용자 쪽이 이긴다.
5. **통째 복사·상시 로드를 제안하지 않는다.** 이 저장소나 트랙 파일을 사용자 지침에 붙이거나 매 세션 읽게 하지 않는다. 규칙은 로딩 조건이 있는 형태로만 넣는다.

---

## 출력 양식 — 항상 이 모양

자유 서술로 요약하지 않는다. 형식이 매번 달라지면 사용자가 이전 진단과 비교할 수 없다.

- 사용자 지침이 응답 머리에 요구하는 것(지침 버전 확인 등)이 있으면 양식 앞에 둔다 — 사용자 규칙이 이긴다(금지 1).
- **양식 밖의 절을 만들지 않는다.** 이 저장소의 트랙이 다루지 않지만 사용자가 알아야 할 것(권한 설정의 위험 등)은 "범위 밖 관찰"에만 사실로 적고, naite 이름으로 제안하지 않는다.
- 제안 한 항목이 길면 여러 줄로 나눠도 된다. 다만 필드 순서(gate ID · 구분 · 넣을 문장 · 위치 · 조건 · 유래 · 충돌)는 지킨다.

```
## naite 진단 — {YYYY-MM-DD} · 수준 {starter|격차|확인 필요} · 경로 {처음|재진단} · 기준 판 {CHANGELOG 맨 위 판}

### 읽은 것
- 사용자 환경: {파일 목록 / 없음}
- 수준 판정: {근거 한 줄} / 질문: {수준이 애매할 때만}
- naite 표시: {N개 · 가장 최신 판 / 없음}
- 연 트랙: {트랙} ({연 이유 한 줄}) / 열지 않은 트랙: {트랙} ({이유})

### 적용분 대조 (재진단일 때만)
| 표시 | 위치 | 적용 판 | 뒤 판의 변화 | 판정 |

### 진단표
| gate | 판정 | 사용자 환경 위치 | 근거 |

### 제안 (상한 5)
- [{gate ID}] {구분: 새 | 갱신 | 제거 질문 | 충돌 질문 | 근거} {넣을 문장·조각} → {파일·위치} · 조건: {검증된 조건} · 유래: {사건형|판단형}

### 보류·해당 없음
- [{gate ID}] {사유}

### 적재 훅
- 형식: YYYY-MM-DD | 기대 | 실제 | 변경 → 위치 제안: {파일 / 이미 있음}

### 범위 밖 관찰 (있을 때만)
- {관찰한 사실} · 위치: {파일·줄} · 사용자가 판단할 것: {한 줄}
```

starter 경로에서는 진단표 대신 starter 항목(S1~S5)마다 "사용자 환경에 이미 있나"를 적고, 제안 줄의 gate ID 자리에 starter 항목 번호를 쓴다(S5는 적재 훅 칸에). 표시도 `(naite S1 · v0.1.0)`처럼 붙인다. 근거 슬롯 제안은 구분을 `근거`로 적고, 인용(파일·줄·원문 한 조각)을 넣을 문장보다 먼저 적는다. 지침 파일을 새로 만드는 제안은 gate ID 자리에 `신설`을 쓴다.
