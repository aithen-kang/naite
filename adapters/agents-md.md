# 어댑터 · AGENTS.md 계열

AI용. `AGENTS.md`를 상시 지침으로 읽는 에이전트 도구에 쓴다.

- 이 계열의 권한·기억·자동 발동 위치는 **이 저장소가 확인하지 않았다.**

| 역할 | 이 계열에서 | 확인일 |
|---|---|---|
| 상시 지침 | 작업 폴더 루트의 `AGENTS.md` | 확인일 없음 — 이 저장소가 직접 확인하지 않았다 |
| 권한 | [⚠️ TBD] | — |
| 설정 반영 시점 | [⚠️ TBD] | — |
| 기억 | [⚠️ TBD] — 자동 기억이 없으면 수동으로 운용할 수 있다: 작업 폴더에 `MEMORY.md`(한 줄 인덱스)와 사실 하나당 파일 하나를 두고, 지침 파일에 "세션을 시작하면 MEMORY.md를 읽는다"를 적는다 | — |
| 자동 발동 | [⚠️ TBD] | — |
| 정해진 시각 실행 | OS 스케줄러 — 도구와 무관 | — |
| 비밀 보관 | env 파일 + 그것을 읽는 모듈 — 도구와 무관 | — |

## AI-START 적용 메모

- **TBD 칸을 추측으로 채우지 않는다.** 사용자 도구의 문서를 확인하거나 사용자에게 묻는다.
- 확인이 안 되면, 넣는 형태가 `설정`·`훅`인 gate는 **지침 파일의 문장**으로만 제안한다. 절차는 도구 기능 없이 문장만으로도 성립하게 쓴다.
- 이 저장소를 사용자 작업 폴더 안에 받지 않게 한다 — 같은 이유로 진입점 이름이 `AGENTS.md`가 아니다.
