# chat — 채팅 AI에서 골라 쓰기

채팅 AI(당신 폴더의 파일을 직접 읽지 못하는 AI)를 쓰는 사람을 위한 한 장입니다. 파일을 읽는 에이전트 도구가 있다면 이 파일 대신 [README.md](README.md)의 한 줄을 쓰세요 — 그쪽은 AI가 당신 환경을 읽고 진단합니다.

채팅 사용자가 복사할 곳은 **여기 하나뿐입니다.** 같은 문장이 `starter.md`와 각 트랙의 `gates.md`에도 있지만 그 둘은 AI가 읽는 파일이고, 이 파일은 사람이 읽고 고르는 파일입니다.

## 어떻게 쓰나

1. **지침 파일이 아직 없으면** 아래 "처음 넣을 것"부터 넣습니다. 있으면 건너뛰고, 당신이 하는 일에 해당하는 절로 갑니다.
2. 항목마다 **언제 쓰는 규칙인지**가 앞에 있습니다. 당신 상황이 아니면 넣지 마세요. "안 통할 때"에 해당해도 넣지 않습니다. 규칙은 많을수록 좋은 것이 아니라, 안 지켜지는 규칙이 하나 생기면 나머지도 같이 흐려집니다.
3. 위아래를 ``` 세 개로 감싼 칸(화면에서는 회색 상자로 보입니다) 안의 문장을 그대로 복사해 프로젝트 지침(또는 커스텀 인스트럭션)에 붙여 넣습니다. 상자 첫 줄이 `##`로 시작하는 제목이면 그 줄도 함께 붙여 넣습니다 — 지침 안에서 규칙 묶음의 이름이 됩니다. 말투는 고쳐도 되지만 조건과 뜻은 바꾸지 마세요.

문장 끝에는 `(naite SPEC-04 · vX.Y.Z)`나 `(naite S3 · vX.Y.Z)`처럼 항목 ID와 판(이 저장소의 버전 번호)을 적어 두세요. 문장이 여러 줄이면 마지막 줄 끝에 한 번만 붙입니다. 판은 [CHANGELOG.md](CHANGELOG.md) 맨 위 `## v…` 줄의 번호입니다 — 그 파일의 나머지는 읽지 않아도 됩니다. 채팅에서는 이 표시가 하는 일이 없지만, 나중에 파일을 읽는 AI 도구로 옮기면 그 도구가 표시를 보고 이미 넣은 문장은 다시 권하지 않습니다.

---

## 처음 넣을 것 — 지침 파일이 없다면

`starter.md`의 다섯 항목 중 채팅에서 온전히 성립하는 것은 S3 하나입니다. 나머지 넷이 왜 빠졌는지는 이 파일 끝에 적었습니다.

### S3 · 모르는 것과 보탠 것을 표시하기

**언제** — 자료가 있는 글을 AI와 함께 씁니다. 나중에 누군가 "이건 어디서 나온 값이지"를 되짚습니다.
**안 통할 때** — 원본이 없는 발상 단계의 창작 초안. 전부가 보탠 것이라 표시가 정보를 주지 않습니다.

```markdown
## 모르는 것과 보탠 것
- 자료에 없는 것은 지어내지 않고 [⚠️ TBD]로 둔다.
- 자료에 없는 것을 보탰으면 [⚠️ AI추론]을 붙인다.
  날짜에서 계산한 요일, 합계, "~는 해당 없다" 같은 파생값도 보탠 것이다.
```

---

## 문서를 쓰고 유지한다

기획·정책·보고 문서를 AI와 함께 쓰거나 고칩니다. 규칙마다 그것이 생긴 사건은 [tracks/spec/cases.md](tracks/spec/cases.md)에 있습니다.

### SPEC-01 · 구조를 먼저, 상세는 확정 뒤에

**언제** — 여러 섹션짜리 문서를 AI에게 새로 쓰게 합니다.
**안 통할 때** — 외부 서식이 구조를 이미 고정한 문서(공모 서식·제안 요청 응답 양식).

```
여러 섹션짜리 문서는 뼈대(목록·상태값)만 먼저 쓰고 내 확정을 받은 뒤 상세를 쓴다. 확정한 구조를 바꿔야 하면 먼저 제안한다.
```

### SPEC-02 · 용어와 상태값을 먼저

**언제** — 한 주제를 여러 문서나 여러 단위로 나눠 씁니다.
**안 통할 때** — 용어 자체를 문답으로 찾아가는 탐색 초기. 먼저 고정하면 틀린 용어가 굳습니다.

```
개별 단위를 쓰기 전에 용어 사전과 상태값 표를 먼저 확정한다.
```

### SPEC-03 · 만들지 않을 것도 근거와 함께

**언제** — 작업량이 산출 단위 수(화면·장·페이지)에 비례하는 문서 작업을 시작합니다.
**안 통할 때** — 산출 단위가 계약으로 고정돼 협상할 범위가 없습니다.

```
시작 전에 만들 것과 만들지 않을 것을 정하고, 제외한 것은 근거와 함께 목록에 남긴다.
```

### SPEC-04 · 축약 금지

**언제** — AI가 쓴 문서를 다른 AI(다음 대화의 AI 포함)가 읽고 무언가를 만듭니다.
**안 통할 때** — 사람에게 보내고 끝나는 요약(메일 본문·보고 한 장).

```
문서에 "원본 참조", "이하 동일"을 쓰지 않는다. 표는 행을 줄이지 않고 전부 옮긴다.
```

### SPEC-05 · 확인은 영역 끝에 한 번

**언제** — AI와 문답하며 문서를 채웁니다.
**안 통할 때** — 반영 즉시 되돌리기 비싼 확정이 나갑니다(외부 발송 문구·게시).

```
문답 중에는 확인을 반복하지 않고 바로 반영한다. 한 영역이 끝나면 채운 것과 빈 것을 한 번 보여 준다.
```

### SPEC-06 · 미정과 AI 추가를 구분해 표시

**언제** — 원본(자료·결정·기록)이 있는 문서를 AI가 쓰거나 고치고, 지침에 **"확인함"처럼 사실을 확인했다는 표시를 이미 쓰고 있습니다.** 첫 줄은 위의 S3와 같습니다. 다른 것은 둘째 줄입니다 — 확인한 문장에서 계산해 낸 값(요일·합계 같은 것)이 그 문장의 "확인함"을 함께 달고 확인된 사실처럼 보이는 일을 막습니다. 그런 표시를 쓰지 않는다면 S3로 충분하니, **둘 중 하나만** 넣으세요.
**안 통할 때** — 원본이 없는 발상 단계의 창작 초안.

```
자료에 없는 것은 [⚠️ TBD]로 두고, 보탠 것에는 [⚠️ AI추론]을 붙인다.
원문에서 계산하거나 끌어낸 값(요일·기간·합계·"해당 없음")은 원문의 확인 표시를 물려받지 않고 따로 표시한다.
```

### SPEC-14 · 관찰한 사실에 확인 날짜

**언제** — 문서가 외부 시스템·데이터·다른 사람의 상태에 관한 사실을 적습니다.
**안 통할 때** — 관찰이 아니라 결정을 적는 문서(정책 규칙 자체). 시점은 버전과 변경 기록이 맡습니다.

```
시스템·데이터에서 확인한 사실에는 확인한 날짜를 붙인다(예: 확인 기준 2026-09-15).
```

### SPEC-19 · AI가 쓴 문서의 검증 상태

**언제** — AI가 쓴 문서가 다른 사람이나 다른 AI가 근거로 읽는 곳에 놓입니다.
**안 통할 때** — 사람 검토 승인을 거쳐야만 저장소에 들어가는 흐름. 그 저장소에 있다는 사실이 곧 검증 상태입니다.

```
AI가 쓴 문서 상단에 "검증됨" 또는 "AI 초안 — 검증 전"을 적는다. 검증 전 초안은 기준으로 삼는 문서에서 링크하거나 인용하지 않는다.
```

## "있다 · 없다"를 적기 전에

아래 넷은 반례가 없는 규칙입니다 — 넣지 않을 상황을 쓸 수 없어서 "안 통할 때" 칸이 비어 있습니다. 조건에 해당하면 그대로 넣으세요.

### SPEC-09 · 개수는 총계로

**언제** — 목록 조회 결과(화면·검색 결과·붙여 넣은 표)의 개수를 결론에 적는 일이 있습니다.

```
목록의 개수를 적을 때는 한 페이지에 보인 수가 아니라 총계를 확인해 적는다.
```

### SPEC-10 · 조회 실패는 부재가 아니다

**언제** — 찾지 못했다는 것을 근거로 "없다"고 적는 일이 있습니다.

```
조회가 실패하거나 비었을 때는 "없다"고 적기 전에 조회 경로가 맞는지부터 확인한다. 에러는 부재의 증거가 아니다.
```

### SPEC-11 · "없다"는 전수로만

**언제** — "없다 · 지원하지 않는다"를 결론으로 쓰고, 그 위에 제안(신설·제거)을 쌓는 일이 있습니다.

```
"없다"는 결론은 전수 확인 뒤에만 쓴다. 있다는 주장은 하나로, 없다는 주장은 전부로 성립한다.
```

### SPEC-13 · 이전 결론은 실행 전에 다시 확인

**언제** — 이전 대화의 결론이나 조사를 맡긴 AI의 권고를 근거로 삭제·치환·판정을 하는 일이 있습니다.

```
이전 대화의 결론이나 조사 AI의 권고로 삭제·치환하기 전에 대상을 직접 다시 확인한다. 조사 결과는 가설이다.
새로 확인한 사실이 이전 결론과 어긋나면 조용히 덮지 않고 "정정"으로 표시한다.
```

---

## 숫자를 다룬다

데이터를 집계해 숫자를 보고서·메시지로 내보냅니다. 조회는 당신이 하고 해석만 AI와 함께 하는 경우에도 아래 문장은 그대로 작동합니다. 사건은 [tracks/numbers/cases.md](tracks/numbers/cases.md)에 있습니다.

### NUM-02 · "기타" 행을 섞지 않기

**언제** — 집계 결과를 항목별로 나눠 비중·추이를 계산합니다.
**안 통할 때** — 원시 표를 직접 집계해 시스템이 꼬리를 뭉치지 않습니다.

```
집계 결과의 "기타"·파싱 실패 행은 실제 항목에 합치지 않고 "기타(시스템 집계)" 줄로 따로 두며, 그 줄이 나타난 날은 변동 요인으로 적는다.
```

### NUM-04 · 진행 중인 기간은 추이에서 뺀다

**언제** — 시간·일 단위 추이를 해석해 보고합니다.
**안 통할 때** — 누적이 아니라 시점 값(동시 접속·재고)을 다룹니다.

```
끝나지 않은 시간 버킷(현재 시간대·오늘, 적재가 덜 끝났을 수 있는 어제)에는 "(진행 중)"을 붙이고 증감 해석에서 뺀다.
```

### NUM-09 · 정의가 둘이면 보고에 쓸 것은 하나

**언제** — 같은 이름의 지표를 여러 방식으로 셀 수 있고, 그 숫자를 반복해서 보고합니다.
**안 통할 때** — 지표 정의가 외부 표준·계약으로 하나로 고정돼 있습니다.

```
같은 지표에 정의가 둘 이상이면 보고에 쓸 정의를 하나 정해 {지표 정의 문서}에 적고, 분모는 그 정의를 따른다. 다른 정의의 숫자는 "참고: …"로 구분한다.
```

### NUM-12 · 적게 잡힌 숫자의 정체

**언제** — 대상이 한 자리·수십 단위로 적게 잡혔는데 그대로 보고하려 합니다.
**안 통할 때** — 테스트 트래픽이 수집 단계에서 분리 적재됩니다.

```
적게 잡힌 대상은 그대로 보고하지 않고, 국가·시점·계정으로 한 번 내려가 테스트 트래픽인지 가린다.
```

### NUM-20 · 이벤트는 상태가 아니다

**언제** — 클릭·시작 같은 이벤트 기록으로 "켜 둠 · 동시 사용" 같은 지속 상태를 판정하려 합니다.
**안 통할 때** — 기록이 시작과 종료를 쌍으로 빠짐없이 남깁니다.

```
이벤트 로그로 지속 상태를 추론한 부분은 한계로 적어 남긴다. 상태가 꼭 필요한 질문이면 "새 계측이 필요하다"를 결과로 낸다.
```

### NUM-25 · 비교할 집단과 관측 창

**언제** — 배포·정책 변경의 영향을 집단 비교로 판정합니다.
**안 통할 때** — 무작위 배정 실험이고 적용이 한 시점에 전량 끝났습니다.

```
영향 분석의 표본은 실패가 일어날 수 있는 집단에서 뽑고, 순차 적용이면 마지막 적용 이후까지 관측 창을 연다. "정상 집단에서 이상 없음"은 결론이 아니다.
```

### NUM-27 · 내보내기 전 한 장

숫자를 사람에게 내보내기 직전에 한 번 훑는 체크리스트입니다. 이 트랙에서 문장으로 따로 넣지 않은 항목 열일곱 개가 여기 줄로 들어 있어, **통째로 가져가는 편이 빠릅니다** — [tracks/numbers/kit/export-checklist.md](tracks/numbers/kit/export-checklist.md).

---

## AI에게 자료와 기억을 맡긴다

채팅 AI라도 자료를 첨부하고, 대화를 넘는 기억을 씁니다. 그 자리에서 성립하는 셋입니다.

### SETUP-10 · 자격증명을 대화와 문서에 적지 않기

**언제** — 외부 서비스의 계정·키를 쓰고, 설정·예제 파일이나 문서를 공유·백업합니다. 반례가 없는 규칙입니다.

```
자격증명(계정·비밀번호·키·서명이 붙은 주소)은 설정·예제·문서에 적지 않고 환경 변수로 참조한다.
평문 자격증명을 발견하면 위치를 알리고, 옮긴 뒤 재발급까지를 조치 완료로 본다.
```

채팅에서는 한 가지가 더 붙습니다 — **대화창에 붙여 넣은 키도 적어 둔 것과 같습니다.** 대화 기록은 남고, 그 기록은 백업·동기화됩니다.

### SETUP-13 · 자료에 있는 것은 기억에 두지 않기

**언제** — 대화를 넘어 남기는 기억 기능을 씁니다.
**안 통할 때** — 원 정보가 AI가 매번 접근할 수 없는 곳에만 있어, 기억 속 사본이 유일한 경로입니다.

```
이미 자료나 파일에 담긴 내용은 기억에 저장하지 않는다. 이번 대화에서만 쓰는 정보도 남기지 않는다.
```

### SETUP-14 · 절대 날짜와 오래된 기억의 대조

**언제** — 대화를 넘어 남기는 기억 기능을 씁니다.
**안 통할 때** — 기억마다 작성 시각이 자동으로 붙고 AI가 그것을 함께 읽습니다. 그때도 둘째 줄은 그대로 넣습니다.

```
기억에 쓰는 날짜는 "어제 · 다음 주" 대신 절대 날짜로 바꾼다.
오래된 기억은 지금의 자료와 대조한 뒤에만 근거로 쓴다.
```

---

## 통째로 가져갈 수 있는 것

도구가 필요 없어 채팅에서도 그대로 쓰는 템플릿·체크리스트입니다.

- [문서 머리 블록](tracks/spec/kit/doc-header.md) — 문서의 상태와 검증 여부를 머리에 고정합니다(S3 · SPEC-01 · 19와 짝).
- [표시 태그](tracks/spec/kit/tags.md) — 미정·AI 추가 표시와, 한 사실에 출처가 여럿일 때의 근거 표기(S3 · SPEC-06 · 14와 짝).
- [용어·상태값 골격](tracks/spec/kit/domain-skeleton.md) — SPEC-02에서 먼저 채울 표.
- [커버리지 보고 양식](tracks/spec/kit/coverage-checklist.md) — SPEC-05의 "한 영역이 끝났을 때" 보고 틀.
- [운영 문서 템플릿](tracks/spec/kit/living-doc.md) — 현재와 개선안을 한 표에 섞지 않는 문서 틀.
- [넘기기 전 점검](tracks/spec/kit/pre-handoff-check.md) — 작업 지시문과 폐기값이 문서에 남았는지, 바꾼 작업에서 옛 값을 검색해 봤는지 훑는 목록.
- [내보내기 전 체크리스트](tracks/numbers/kit/export-checklist.md) — 숫자를 내보내기 전 여덟 축.
- [출처 꼬리말](tracks/numbers/kit/source-footer.md) — 숫자에 기준·기간·정의를 붙이는 한 줄 틀.
- [세 모델로 말하기](tracks/numbers/kit/three-models.md) — 추정을 현실·비관·상한으로 나눠 보고하는 틀.

## 파일을 읽는 도구가 있어야 되는 것

채팅에서는 넣어도 지켜지지 않아 뺐습니다. 도구를 쓰게 되면 [README.md](README.md)의 한 줄로 시작하세요.

- **starter S1 · S2 · S4** — 작업을 시작하는 순서, 파일 조작의 세 단계, 만든 것의 확인 절차. AI가 파일을 만들고 명령을 실행한다는 전제 위에 있습니다.
- **starter S5 기대가 어긋난 순간 기록** — 기록하는 것 자체는 채팅에서도 되지만, 규칙의 뒷부분("같은 유형이 두 번 적히면 AI가 규칙으로 올릴지 묻는다")은 AI가 당신 기록을 읽을 수 있어야 지켜집니다. 반쪽만 지켜지는 규칙을 지침에 넣지 않도록 뺐습니다.
- **spec** — SPEC-07(문서 머리 관리) · 08 · 12(시스템·코드에서 확인) · 15 · 16(문서 대조 스크립트) · 17 · 18(게시 자동화).
- **setup 나머지** — 권한 설정, 자격증명 보관 구조, 반복 요청의 도구화.
- **automation 전체** — 스케줄러·상주 프로세스·봇을 직접 운영할 때의 규칙이라 채팅에서 가져갈 문장이 없습니다.
