# starter — 지침 파일이 없는 사람을 위한 최소셋

> 채팅 AI만 쓰는 사람은 이 파일이 아니라 [chat.md](chat.md)를 보세요 — 여기 다섯 항목 중 채팅에서 온전히 성립하는 것은 S3 하나입니다.

AI용. `AI-START.md` 단계 2에서 여기로 왔다면, 격차 진단 대신 아래 다섯 항목 중에서 제안한다. 상한·출력 양식·승인 절차는 `AI-START.md`와 같다.

넣을 위치는 어댑터의 "상시 지침" 행이다. 지침 파일이 아예 없으면 새로 만드는 것 자체를 첫 제안으로 올린다. 이 제안은 상한에 세지 않는다. 넣을 문장은 사용자의 말투로 다듬어도 되지만, 조건과 뜻은 바꾸지 않는다.

**트랙 항목과의 대응** — starter 표시가 붙은 항목은 격차 진단과 재진단에서 대응 gate를 **있음**으로 본다.

| starter | 대응 항목 |
|---|---|
| S1 작업을 시작하는 순서 | SETUP-22 |
| S2 파일을 다룰 때의 세 단계 | SETUP-01 |
| S3 모르는 것과 보탠 것 | SPEC-06 |
| S4 확인까지 주문에 넣기 | SETUP-18 |
| S5 기대가 어긋난 순간 기록 | 트랙 항목 없음 — `AI-START.md` 단계 7 |

---

## S1. 작업을 시작하는 순서

```markdown
## 작업을 시작할 때
1. 이 지침 파일을 먼저 읽었음을 확인한다.
2. 이 작업과 관련된 템플릿·규약 파일이 있으면 읽는다.
3. 이미 만들어 둔 파일이 있으면 읽고 이어서 작업한다.
4. 그다음에 작업을 시작한다.
```

- **검증된 조건:** AI가 파일을 만들고, 다음에 그 파일을 이어서 쓰는 일을 반복한다.
- **안 통하는 상황:** 파일을 남기지 않는 일회성 질문만 한다.
- **유래:** 판단형 — 템플릿을 무시하고 자기 구조로 쓰거나 기존 파일을 처음부터 다시 만드는 일을 막으려는 순서다.

## S2. 파일을 다룰 때의 세 단계

```markdown
## 파일을 다룰 때
- 그냥 해도 된다: 읽기, 새 파일 만들기, 결과물 갱신
- 먼저 묻는다: 파일·폴더의 삭제나 이동, 이 지침 파일과 설정 파일 자체의 변경, 외부로 보내기
  — 무엇을 왜 바꾸는지 1~2줄로 말하고 진행 여부를 묻는다
- 하지 않는다: 되돌릴 수 없는 삭제, 외부 시스템의 삭제 호출
판정 기준은 "위험해 보이는가"가 아니라 "되돌릴 수 있는가"다.
```

- **검증된 조건:** 덮어쓴 파일을 되살릴 수단(버전 관리·백업)이 있다. **없으면** "결과물 갱신"을 "먼저 묻는다"로 옮겨서 제안한다.
- **안 통하는 상황:** AI에게 읽기만 맡기고 파일 쓰기를 전혀 시키지 않는다 — 다룰 파일 조작이 없다.
- **유래:** 판단형.

## S3. 모르는 것과 보탠 것을 표시하기

```markdown
## 모르는 것과 보탠 것
- 자료에 없는 것은 지어내지 않고 [⚠️ TBD]로 둔다.
- 자료에 없는 것을 보탰으면 [⚠️ AI추론]을 붙인다.
  날짜에서 계산한 요일, 합계, "~는 해당 없다" 같은 파생값도 보탠 것이다.
```

- **검증된 조건:** 원본(자료·결정·시스템)이 있는 문서를 AI가 쓴다. 누군가 나중에 표시를 검색해 확인한다.
- **안 통하는 상황:** 원본이 없는 발상 단계의 창작 초안 — 전부가 보탠 것이라 표시가 정보를 주지 않는다.
- **유래:** 판단형(구분 원칙) · 사건형(파생값) — spec 트랙 SPEC-06과 같은 규칙이다. 표시는 다른 starter 항목과 같이 `(naite S3 · 판)`으로 붙인다.

## S4. 만들어 달라고 할 때 확인까지 주문에 넣기

```markdown
## 무언가를 만들었을 때
"다 됐다"고 보고하기 전에 네 가지를 거치고 결과를 함께 보고한다.
1. 문법 검사
2. 작은 입력으로 한 번 실행
3. 실제로 열거나 돌려 보기
4. 검사나 테스트를 만들었다면 일부러 깨뜨려서 잡히는지 보기
```

- **검증된 조건:** AI가 코드·스크립트·설정·실행되는 파일을 만든다. 주문에 확인이 없으면 AI는 확인을 건너뛴다.
- **안 통하는 상황:** 실행할 것이 없는 글 산출물만 만든다 — 그 경우 S3의 표시로 검수한다.
- **유래:** 사건형 — 문서 검사기를 만들고 일부러 깨뜨려 보자, 검사기 자신의 버그가 그 자리에서 나왔다.

## S5. 기대가 어긋난 순간을 한 줄 적기

```markdown
## 기록
기대와 다르게 흘러간 일이 있으면 {파일}에 한 줄 적는다.
YYYY-MM-DD | 기대: ___ | 실제: ___ | 변경: ___
같은 유형이 두 번 적히면 이 지침 파일의 규칙으로 올릴지 사용자에게 묻는다.
```

- **검증된 조건:** 같은 종류의 일을 반복한다.
- **안 통하는 상황:** 반복하지 않는 일회성 작업만 한다.
- **유래:** 판단형. `AI-START.md` 단계 7과 같은 항목이므로, starter 경로에서는 적재 훅 칸에 이 항목을 쓴다.
